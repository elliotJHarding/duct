# A ticket, up close

$intro

~~~~text
$anatomy
~~~~

The first lines of `TICKET.md`:

~~~~markdown
$excerpt
~~~~

The `source: sync` frontmatter marks a file as sync's — it is rewritten on
every sync, so don't edit it. Everything else in the folder is yours:
notes, specs, scratch files, agent output. Sync never touches them.
